<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">YelpCamp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/">Home</a>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Campgrounds
          </a>
          <ul class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown">
            <li><a class="dropdown-item" href="/campgrounds">All Campgrounds</a></li>
            <li><a class="dropdown-item" href="/campgrounds/new">New Campground</a></li>
          </ul>
        </li>
      </div>
      <% if(currentUser && currentUser.isAdmin ) { %>
      <a class="nav-link" href="/admin">Panel</a> <% } %>
      <div class="navbar-nav ms-auto"></div>

      <form action="/campgrounds/search" method="GET" class="d-flex flex-grow-1 mx-lg-5">
        <input
          name="search"
          id="search"
          type="search"
          placeholder="Search Campgrounds"
          aria-label="Search"
          class="form-control w-100 me-2 bg-dark text-white btn-outline-secondary"
        />
        <button class="btn btn-outline-secondary" type="submit" aria-label="search"><i class="bi bi-search"></i></button>
      </form>

      <% if(!currentUser) { %>
      <a class="nav-link" href="/login">Login</a>
      <a class="nav-link" href="/register">Register</a>
      <% } else { %>
      <li><a href="/users/<%=currentUser.id%>">Signed In As <%= currentUser.username %></a></li>
      <a class="nav-link" href="/logout">Logout</a>
      <% } %>
    </div>
  </div>
</nav>
